# Waxcelerate Website · Einstiegspunkt

Diese Datei ist das Inhaltsverzeichnis. Sie soll kurz bleiben, damit jede neue
Session sie ganz liest, ohne Kontext zu verbrennen. Alles Ausführliche liegt in
`docs/` und wird nur bei Bedarf geöffnet.

**Regel für Agenten:** diese Datei immer lesen. Aus `docs/` nur das, was der
konkreten Aufgabe entspricht. Neues Wissen gehört in die passende Detaildatei,
nicht hierher, außer es ist eine Entscheidung oder ein offener Punkt.

---

## Was das ist

Marketing-Website für Waxcelerate, deutsche Kleinmarke für Heißwachs-Kettenpflege
(Luca Teichmann, Stuttgart). React 19, TypeScript, Vite, kein Server.
Verkauf über eBay und Direktbestellung. Live auf `waxcelerate.de`, Deploy über
Push auf `main`, Vercel baut automatisch.

---

## Wie die Doku sortiert ist

Im Wurzelverzeichnis liegen nur vier Dateien, und das soll so bleiben. Alles
andere steht in `docs/`, nach Lebenszyklus getrennt:

| Ort | Was dort liegt |
|---|---|
| Wurzel | `PROJECT.md` (diese Datei), `CLAUDE.md`, `AGENTS.md`, `README.md` |
| `docs/` | Dauerhafte Referenz: Designsystem, Audit, Repo-Karte, Wissenschaftsnotizen |
| `docs/plaene/` | Laufende Bauvorhaben. Ist eines fertig, wandert es nach `docs/archiv/` |
| `docs/aufgaben/` | Was **Luca** selbst tun muss, nicht ein Agent |
| `docs/archiv/` | Abgearbeitet oder überholt. Nur als Muster oder Beleg lesen, nie als Auftrag |

**Neue Datei anlegen:** in den passenden Unterordner, und eine Zeile in die
Tabelle unten. Keine neuen Markdown-Dateien in die Wurzel.

---

## Wo was steht

| Thema | Datei | Wann lesen |
|---|---|---|
| Technischer Einstieg, Deploy, Fallen | `AGENTS.md` | vor der ersten Code-Änderung |
| Kurzregeln, Datenhaltung, Hooks | `CLAUDE.md` | immer |
| Repo-Karte, Nachbarordner, Git-Stashes | `docs/REPO_KARTE.md` | bei Git- oder Ordnerfragen |
| **Designsystem, Farben, Zeichnungen, Typo** | **`docs/DESIGN.md`** | bei allem Visuellen |
| **Audit der ganzen Seite, Befunde und Reihenfolge** | **`docs/AUDIT.md`** | bei Struktur-, Funnel- und Prioritätsfragen |
| Wissenschaftsseite und Startseite, aktueller Umbau | `docs/WISSENSCHAFT_NOTES.md` | bei Arbeit an diesen Seiten |
| **Wissenschaftsseite: Faktenprüfung und Umbaukonzept (09/2026)** | **`docs/plaene/WISSENSCHAFT_REDESIGN.md`** | **vor jeder Arbeit an `/wissenschaft`** |
| Blog, Stand und Konzept | `docs/plaene/BLOG_PLAN.md` | bei Blogarbeit |
| Blog, abgearbeitete Aufgaben | `docs/archiv/BLOG_EXECUTION.md` | als Muster |
| Rechtstexte, Stripe, UX-Plan | `docs/aufgaben/RECHTSTEXTE.md`, `docs/aufgaben/STRIPE_SETUP.md`, `docs/plaene/UX_UPGRADE_PLAN.md` | themenbezogen |
| Navigation, Usability, Conversion — Recherche und Plan | `docs/plaene/NAVIGATION_UX_PLAN.md`, `docs/plaene/CONVERSION_UX_PLAN.md`, `docs/plaene/DISCOVERY_UX_PLAN.md` | bei Navigations-/Bedienbarkeits-/Conversion-Arbeit |
| **Gesamtübersicht: was aussteht, über alle Pläne** | **`docs/plaene/GESAMTUEBERSICHT.md`** | **immer zuerst, bevor du in Einzeldokumente gehst** |
| Mobile-Optimierung (Stufe A/B abgeschlossen, C wartet auf Stripe-IDs) | `docs/plaene/MOBILE_PLAN.md` | bei Arbeit an Mobile-Performance/A11y |
| SEO-Gesamtplan, Reichweite, PFAS-Positionierung | `docs/plaene/SICHTBARKEIT_PLAN.md`, `docs/plaene/REICHWEITE_STRATEGIE.md`, `docs/plaene/PFAS_STRATEGIE.md` | bei SEO-/Reichweiten-Arbeit oder der PFAS-Entscheidung |
| **SEO-Technik: indexierbare URLs, Sitemap/Schema-Erzeugung, Post-Release-Checks, GSC** | **`docs/SEO_TECHNIK.md`** | **bei einer neuen Route oder vor/nach einem Release** |
| Letzter SEO-Prüfbericht (Lighthouse, Rich Results, Redirect-Ketten) | `docs/berichte/` | bei Fragen zum gemessenen Ist-Zustand |
| Design-/Struktur-Roadmap (konsolidiert aus AUDIT/DESIGN/MOBILE/UX) | `docs/plaene/DESIGN_ROADMAP.md` | bei größeren Struktur-/Designvorhaben |
| **Produktkarten, Kettenseite, Typografie — Plan + fertiger Umsetzungs-Prompt** | **`docs/plaene/PRODUKTKARTEN_PLAN.md`, `docs/plaene/PRODUKTKARTEN_PROMPT.md`** | bei Arbeit an Produktkarten, Kettenliste, Filter oder der Mono-Schrift |
| Rechtliche Compliance, aktuelle Checkliste | `docs/aufgaben/LEGAL_COMPLIANCE_CHECKLIST.md` | bei Rechtstext-/Checkout-Arbeit (neuer als `RECHTSTEXTE.md`) |

Inhaltliche Wahrheit über Produkt, Preise, Intervalle und erlaubte Claims steht
**nicht im Repo**, sondern im Anthropic-Skill `waxcelerate`. Ist es nicht
installiert: nachfragen statt raten. Nie eine technische Zahl aus dem Code
übernehmen, ohne sie zu hinterfragen.

---

## Offene Entscheidungen, die nur Luca treffen kann

Kurz halten. Erledigtes wandert nach unten in den Log.

- **µ 0,03 ist ein Vakuumwert.** Neu geprüft am 15.09.2026: MoS₂ erreicht µ ≈ 0,03 nur im Vakuum oder Inertgas, in feuchter Luft liegt es bei 0,15 bis 0,30. Damit ist die Zahl nicht nur „der beste Wert der Spanne", sondern stammt aus einer Umgebung, in der die Kette nie läuft. Entscheidung offen: ganz von der Seite nehmen oder nur mit Umgebungsangabe im MoS₂-Kapitel. `docs/plaene/WISSENSCHAFT_REDESIGN.md` §1.1
- **Wattzahlen: `data.ts` steht jetzt auf 250 W** (korrigiert am 15.09.2026, die zitierten ZFC-Werte entstehen bei 250 W). Offen bleibt zweierlei: die Claims-Tabelle im Skill nennt weiterhin 300 bis 400 W, und vier Stellen im Blog plus eine in der FAQ berufen sich auf „**unsere Laborreferenz** bei 300 bis 400 W". Eine eigene Laborreferenz kennt der Skill nicht. §1.2 und §7a
- **Hat Classic FT-Wachs und Antioxidans?** `LineChoice` sagt „dieselbe Wachsmatrix", `diveFormula('classic')` sagt ohne FT-Wachs und ohne Antioxidans. Beides ist live. Davon hängt auch das Temperaturfenster ab. §1.5
- **Herkunft der acht Mikroskopaufnahmen.** Ungeklärt, werden aber als Beleg gezeigt. Bildrechte und § 5 UWG. Vorschlag: aussetzen, bis geklärt. §1.6
- **„~10 nm Lamellen"** im Paraffin-Text. Periode rund 9,3 nm ist ein Doppelstapel, eine Lamelle 4 bis 5 nm.
- **PTFE- und PFAS-Sprachpolitik** ist offen und blockiert einen geplanten Blogartikel.
- **„Leiser Antrieb"** wäre ein starkes Verkaufsargument, steht aber in keiner verbindlichen Quelle. Widerspruch: `docs/aufgaben/SKILL_PREISE_UPDATE.md` nennt ihn „freigegeben (29.07.2026)". Luca am 15.09.2026: bleibt auf beiden Seiten draußen, bis das geklärt ist. Zwei echte Bewertungen (`reviews.tsx`) sagen „leiser".
- **Foto-Lücke:** kein eigenes Vorher-Nachher-Paar geölt gegen gewachst. Größter offener Hebel auf der Startseite. `ComparisonSlider` liegt fertig und ungenutzt.
- **Kaufweg:** eBay bis der Stripe-Checkout steht, geplant Sonntag oder Montag.
- **Startseitenlänge:** Rechner, Anleitungen und FAQ stehen nach dem Kaufangebot. Vorschlag in `docs/AUDIT.md`, bewusst noch nicht umgesetzt.
- **Kassetten- und Kettenpreise für 8- bis 10-fach.** Der Verschleiß-Rechner nennt Kosten nur für 11/12-fach (unsere Ketten, XT-Kassette 130 €). Bei 8–10-fach steht bis zur Entscheidung nur, *was* fällig ist („Kette, evtl. Kassette"), ohne Betrag.
- **Ketten-Kit-Rabatt 5 % / 10 %** stand fest verdrahtet im alten Ersparnis-Rechner, ohne Grundlage in `data.ts` (dort gibt es nur die Wachs-Staffel). Der neue Kosten-Rechner rechnet ohne. Gibt es einen Ketten-Rabatt, gehört er nach `data.ts`.
- **Öl-Kosten `OIL_PRICE_PER_APP` 1,10 € je 1.000 km** sind sehr niedrig (real wird eher alle 200–400 km nachgeölt, bei Nässe öfter). Die Annahme ist zugunsten von Öl gesetzt, also vorsichtig — nicht falsch, aber sie drückt die Ersparnis.
- **66 Gedankenstriche** als Satzzeichen in `src/lib/data.ts` sind noch nicht bereinigt. (Stand 11.08.2026 nachgezählt, vorher stand hier 57.)
- **Trockenintervall-FAQ vs. Rotations-FAQ:** die „Wann muss ich rewaxen"-FAQ nennt jetzt 300 km als Empfehlung (400–500 km als das, was auch noch hält), die Rotations-FAQ („Warum lohnen sich mehrere Ketten") rechnet ihr Beispiel weiterhin mit 400–500 km als Einzelketten-Basis (1.200–1.800 km bei drei Ketten). Nur die erste wurde bei der Rechner-Überarbeitung angeglichen; die zweite absichtlich nicht, um nicht in einer Session zwei verschiedene Zahlenkorrekturen an FAQ-Texten vorzunehmen.

Am 11.08.2026 aus dieser Liste entfernt, weil beim Nachprüfen längst erledigt:
`chain-dirty.jpg` existiert nicht mehr, und die Starter-Sets sind live —
`SETS_LIVE` steht auf `true`, `starterSet` liegt in `data.ts`.

---

## Beauftragt, noch nicht gebaut

**Bewertungen: Fotos in die Karten**
Marquee bleibt, das gefaellt. Offen ist, wie das Foto in die Karte kommt, ohne
dass der Text unlesbar wird. Vorschlag: kleines quadratisches Foto links neben
dem Zitat statt Hintergrundbild, damit der Kontrast nicht vom Motiv abhaengt.

**Dunkler Block Herkunft und Produktion**
Konzept siehe Antwort im Chat vom 2026-07-29.

**Skill `waxcelerate` nachziehen**
Fertiger Copy-Paste-Block liegt in `docs/aufgaben/SKILL_PREISE_UPDATE.md`. Das Skill ist
aus der Session heraus nur lesbar, deshalb muss der Block von Hand hinein.

---

## Entscheidungslog

Eine Zeile pro Entscheidung, neueste oben. Begründungen stehen im Code-Kommentar
an der Stelle, an der die Entscheidung wirkt.

- 2026-09-16 · **Nur echte Bewertungen, Fotos zurück** (Branch `feat/echte-reviews`, Luca). `REVIEWS` enthält nur noch Texte, die wortgleich im öffentlichen eBay-Profil stehen (12 Stück, Namen maskiert wie bei eBay, Zeitraum statt Monat). Die Web-Bewertungen Maximilian M., Philippe V. und Michael W. sind raus (nicht echt, Luca). Fotos hängen nicht mehr an Personen: `REVIEW_PHOTOS` (ride-1…5) mit „Foto: Waxcelerate“ klein und weiß im Bild, auf der Startseite an jeder zweiten Karte, auf Wachs- und Kettenseiten an der großen Karte. Die Proof-Leiste zeigt kein Foto mehr neben dem Namen. `Review.id` statt Namenssuche (RewaxPage). Offen: eBay-Zeiträume veralten, bei Gelegenheit neu abgleichen.
- 2026-09-16 · **Der Wachs-Klick im Hero beantwortet die Kaufentscheidung statt der Chemiefrage** (Branch `claude/eager-maxwell-mowkkj`, Luca: „nicht überzeugt, man kommt auch über die Wissenschaftsseite, trägt das zur Conversion bei?"). Der Klick öffnete „Die Formel unter der Lupe" — dieselben sechs Komponenten stehen ausführlicher auf `/wissenschaft`, und eine Rezeptur beantwortet nicht die Frage, mit der jemand vor einem Kauf steht. Jetzt trägt `WaxDive.tsx` zwei Reiter: **„Für mich"** (neu, `WaxVerdict.tsx`) und **„Was drin ist"** (der bisherige Inhalt, 1:1 nach `WaxFormulaPanel.tsx`, nichts verloren). Das Urteil fragt Gelände, Wetter und km/Woche und antwortet mit einem Produkt, einem Begründungssatz aus den vorhandenen `data.ts`-Beschreibungen, zwei Kennzahlen und einem Kauf-Button. **Kein siebter Rechner:** die Auswahl (`src/lib/waxRecommendation.ts`, rein, ohne JSX wie `toolRegistry.ts`) rechnet nichts selbst, sondern liest `switchEconomics`/`applicationsPerBlock`/`costPerApplication` aus `waxMath.ts` und `waxIntervals` — dadurch kann sie dem Rest der Seite nicht widersprechen. **Zwei Hinweise schlagen gegen den größeren Warenkorb aus** und sind Absicht: `outlastsShelfLife` empfiehlt den kleineren Block (7 € weniger), `needsHybridHint` rät zu Heißwachs plus Tropfwachs statt zu mehr Produkt. **Öl-Zweig:** wer angibt, noch Öl zu fahren, bekommt das passende Starter-Set („Den schwierigen Teil haben wir schon gemacht") — die Erst-Entfettung ist laut Recherche die meistgenannte Umstiegsbremse, nicht der Preis; Link auf `/starter-set`, nicht in den Warenkorb, weil Sets bei inaktivem Stripe keinen eigenen eBay-Artikel haben. **Nebenwirkung mit Absicht:** der Dialog schreibt über `useToolProfile` in das seitenweite Fahrprofil, das es längst gab, das aber niemand befüllte — nach dem Hero-Klick stehen alle sechs Rechner und die Produktseiten vorausgefüllt (end-to-end geprüft gegen `/rechner/umstieg`). Eine neue Grafik: `WaxBlockBar` in `sketches.tsx`, der Block geteilt in seine Wachsgänge, der Jahresverbrauch getönt; sagt ehrlich „Block vorher leer", wenn ein Jahr mehr als einen Block frisst. Neues Ereignis `wax_verdict` (einmal je Seitenaufruf, die zwölf bestehenden unverändert). Beschriftung der drei Einstiegspunkte plus Lupe: „Blick ins Wachs" → **„Welcher ist deiner?"**. **Bewusst nicht:** kein zweiter Moment (UX_UPGRADE_PLAN §7.5), keine neue Zahl, kein i18n-Umbau (`WaxDive`/`WaxLensCutout` nutzten `i18n.ts` noch nie, das bliebe ein eigenes Vorhaben). **Offen für Luca:** `waxIntervals` in `data.ts` rechnet trocken/Straße mit 300 km, der verbindliche Claim im Skill lautet 400–550 km (Decision Log D1) — der Widerspruch ist älter als dieser Umbau und wird hier nur an einer weiteren Stelle sichtbar.
- 2026-09-15 · **Kettenseite v1 + CN-HG93 (9-fach)** (Branch `feat/kettenseite-v1`, Luca: „dasselbe wie die Wachsseiten auch für die Ketten“). Ketten bekommen einen eigenen Komponentenbaum `src/pages/product/chain/` nach dem Muster von `wax/`. Produktneutrales kommt unverändert von der Wachsseite: Proof-Leiste, Kapitel 01, Reibungslupe mit Zahnflanke, Rechner (`mode="chain"`: rechnet mit dem Preis der Kette, Kassette als Richtwert der Klasse), Stimmen, FAQ (`kind="chain"`). Neu aus Lucas eBay-Beschreibung „Das Blatt“: „Das bekommst du“ in der Kaufbox, Kapitel „Was wir damit machen“ (4 Schritte, 3 Reinigungsstufen), „Passt sie an dein Rad?“ (`#passung`, mit Grenzen und Geschwisterketten derselben Gangzahl), „Daten, Einbau, Grenzen“, „Wenn das Wachs nachlässt“ (Rewax-Preise aus `rewax/content.ts`) und die Nachhaltig-Karte. **Bewusst nicht aus eBay übernommen:** „Es wird leise“, die REM-Bilder, „400–550 km“, feste Rewax-Preise; die Reibungszahl ist auch in den Ketten-Kennzahlen raus („0 min Vorbereitung“, wie bei Wachs am 14.09.). Die Kachel „< 1 Kette“ gilt jetzt nur für Wachs. **9-fach:** `chain-hg93` (39,95 €), Matrix, `/ketten`-Filter und Ketten-Finder kennen 9-fach. Der Rechner fällt dort weg, weil der Kassettenpreis für 8–10-fach offen ist (siehe oben); die Kapitel zählen automatisch nach. **Runde 2 (Lucas eBay-Screenshots):** HG93-Foto = sein eBay-Hauptbild („1 Chain.JPG“, Varianten wie die anderen Ketten, `hg93` in `CHAINS`). Drei Reinigungsstufen bestätigt, `v9SharedProcessSteps` angeglichen. **Mengenrabatt für Ketten** wie auf eBay: `CHAIN_TIERS` 2 Stk. 5 %, 3 Stk. 7 %, ab 4 Stk. 10 %, Mengenwahl in der Kaufbox. Rückgabe: Die Website bleibt bei 14 Tagen, obwohl eBay „Privat · Keine Rückgabe“ zeigt (Luca). **Offen (Luca):** die eBay-Artikelnummer der HG93 (Platzhalter: Shop-Suche „HG93“). **Offener Widerspruch:** Der eBay-Generator (`ebay-listings/build_listing.py`) nennt 400–550 km als Intervall, die Website 300–550 km. Die Website bleibt vorerst so. Die HG95 (10-fach) folgt erst, wenn sie auf eBay live ist (Masterplan P07). Die alte Inline-Seite in `ProductDetailPage` bleibt für die `bundle`-Produkte.
- 2026-09-15 · **Rewax v3: kürzer, stärkeres Angebot, Orts-Werkzeug** (Branch `feat/rewax-v3`, Luca: „weniger Scrollen, Conversion nach Kaufpsychologie"). Leitidee Hormozis Value Equation: Wartezeit und Risiko senken, Ergebnis greifbar machen. (1) **Bearbeitung fest 3 Werktage** (vorher 3–5), Tür zu Tür 5–7; neues `rewax/dates.ts` rechnet das konkrete Rückgabe-Fenster („Heute eingeworfen → zurück ca. Di 22.–Do 24.9.") mit Feiertagen je Bundesland, Mo–Fr (Samstagszustellung = Puffer). (2) **Mengenpreis ab zwei Ketten** („Rotation": eine fährt, eine ist bei uns — Antwort auf „dann steht mein Rad"). (3) **Leise-Garantie** (`GUARANTEE`): kostenlos nochmal wachsen, Meldung binnen 14 Tagen. (4) **Preise direkt nach dem Hero** als zwei Angebotskarten mit Mengen-Umschalter statt 13-Zeilen-Tabelle; „Anmelden" übergibt Leistung + Menge ans Formular (Fenster-Event `rewax:book`). (5) **PLZ-Werkzeug** statt Städte-Wetter: 95 Leitregionen (`rewax/plzRegions.ts`), `api/weather.ts` nimmt jetzt auch `lat/lon` (auf DE begrenzt) und liefert DWD-Warnungen + Regenwahrscheinlichkeit; Jahresklima ehrlich über die nächste der 12 DWD-Referenzstädte. Rückgabe-Fenster und Intervall funktionieren auch ohne Wetter-API. (6) Stadt-Chips standen doppelt, jetzt einmal als Textlinks unter der FAQ; Ablauf-Fotos 3:2, Vertrauen kompakter, Zitat unter dem Formular; FAQ ab lg zweispaltig, Preiskarten mobil mit Umschalter. **Mobil-Runde (v3.1):** Hero-Bild 260 px, Nutzen als eine Zeile → Formular-Schritt 1 im ersten Bildschirm; Preiskarte ohne doppelten Kopf; Sticky-CTA mit „ab 15,95 € · Leise-Garantie · zurück ca. …"; PLZ-Werkzeug ohne Doppel-Intervall, Regenbalken erst ab sm; Starter-Set mobil flach; FAQ mobil 5 + „Alle zeigen". Fehler behoben: Dativ bei 3 Werktagen („innerhalb von 3 Werktagen", „Zurück in 3 Werktagen" via `TURNAROUND.within/dative`), Hamburg-FAQ sagte „eine Kette im Maxibrief zurück" (richtig: Großbrief), Rückgabezeile nach 17 Uhr „Morgen eingeworfen", PLZ-Hinweis bei einer Ziffer. **Gemessen gegen live:** Desktop 1024 px 6.193 → 5.747 px (−7 %), mobil 375 px 8.820 → 7.807 px (−11 %). Die geplanten 30–40 % sind nicht erreicht, weil PLZ-Werkzeug und zwei FAQ dazukamen; größter Restblock mobil: Geschenk ≈ 1.500 px. **Offen für Luca:** Garantie-Wortlaut und Preis für genau 2 Ketten bestätigen (`LUCA_TODO.md` #8).
- 2026-09-15 · **Ratgeber-Hub v3** (Branch `feat/ratgeber-hub-v3`, Luca: „Hintergrundbild zu dunkel, Lernpfad-Karten liest keiner, Antrieb spiegelverkehrt und physikalisch falsch“). **Kopf hell:** `wax-bath-hanging` (Kette über dem Wachstopf, Stuttgarter Hügel) statt `hero-chain-texture.jpg` unter Scrim, Aufbau wie der Rewax-Kopf (Foto rechts per Maske, mobil oben), H1 in Fraunces statt Libre Franklin Black. **Lernpfad:** Fotokarten aus `getArticleImage`, Etappenziffer, „Hier starten“, Streckenlinie mit Fortschritt, mobil Snap-Karussell. **Wegweiser neu gezeichnet:** Kettenblatt rechts, Kassette links, Kettenbahn aus Kreistangenten mit S-Schleife durchs Schaltwerk, Räder drehen im Übersetzungsverhältnis, Kette läuft in Fahrtrichtung (Material und `sprocketPath` aus `sketches.tsx`). Panel trennt „Wo du es merkst“ / „Wo die Ursache sitzt“, betroffene Bauteile leuchten. **Fakten korrigiert:** Rost-Tipp „alle 100–150 km bei Streusalz“ gehört zum Winter-Artikel, jetzt der Text aus dem verlinkten Abschnitt (trockenreiben, MoS₂); „Springt“ nennt jetzt auch den Normalfall frisch gewachst (verklebte Glieder, Abschnitt 3). **Reihenfolge:** Zahlen vor dem Slider, Stats-Zeile der Empfehlung gestrichen (doppelt), Zahlen-Ziffern kleiner als die H2. **Mobil-Politur** (Luca: „alles prüfen, dann live“): Archiv unter `sm` als kompakte Liste (8553 → 2725 px bei 375 px), Zahlen 2×2, Symptom-Chips als wischbare Zeile (`min-w-0` auf der Karte, sonst wächst die Grafik auf 673 px), Trefferflächen `py-2 -my-2`; Seite mobil 14.886 → ~8.700 px. **Bewusst nicht:** die überlappenden Labels im Vergleichsslider bei 375 px, die kommen aus `BeforeAfterSlider` (auch Startseite).

- 2026-09-15 · **Wissenschaftsseite rechnet mit** (Branch `feat/wissenschaft-live`, auf PR #34). Neues `ProofInstrument` in ACT III: Fahrprofil (`useToolProfile`, geteilt mit Produktseite und Rechnern) → Ersparnis 3 Jahre, Wachsintervall, Wachsgänge über `drivetrainCosts()`, Annahmen aufklappbar. **Ein Kostenmodell pro Seite:** die statische „~47 € auf 12.000 km“-Kachel im Reibungs-Panel ist raus, weil das Instrument darunter beim Standardprofil ≈ 127 € für dieselbe Strecke rechnet (47 € ist gegenüber `waxMath.ts` Stand 14.09. veraltet, Startseite nutzt es weiter, offen). Dispergiersystem zeigt die Stokes-Rechnung als `CalcTrace` (v = 2·Δρ·g·r²/9η, aus den Konstanten berechnet ≈ 1,0 mm/min); Texte von „~0,8“ auf „rund 1 mm/min“ angeglichen. Dichte-Grafik mit Maßlinie, ColdFlex-Caption −10 → −8 °C. **Bewusst nicht:** Querverweis MoS₂ → Mikroskop (Luca), solange die Herkunft der Aufnahmen offen ist (REDESIGN §1.6).
- 2026-09-15 · **Ratgeber-Hub v2 „Frag die Werkstatt“** (Branch `feat/ratgeber-hub-v2`, Luca: „Suche viel smarter, Waden-Bild weg, Blogseite neu erfinden“). Basis: aus PR #33 nur die Suche (MiniSearch, 456 Alias-Phrasen, Synonyme, deutscher Tokenizer) und das Entfernen des Waden-Fotos per Cherry-Pick übernommen; Kartengitter und „Du merkst es sofort“ bleiben in PR #33. **Suche neu:** Tippfehlerkorrektur gegen den Artikel-Wortschatz („quitscht“ → „quietscht“), **Antwortkarten aus den vorhandenen FAQ-Einträgen** (91 Antworten, wortgleich Lucas Texte, nichts generiert; statt einer eigenen `answers.ts`, weil die FAQ schon da und gepflegt ist), Treffer springen per Anker in den passenden `<h2>`-Abschnitt (`headingId.ts`, in Seite, Index und Prerender identisch), „Meintest du …?“, ⌘K/„/“, Pfeiltasten + Enter, letzte Suchen (localStorage), Null-Treffer-Event `search_no_result` (Vercel Analytics). Index v2 nach Abschnitten, 43 KB gzip, lädt erst bei Fokus. **Embeddings bewusst verworfen:** kleinstes mehrsprachiges Modell ≈ 118 MB. **Seite neu:** Suche als Hero mit sich selbst tippenden Beispielfragen, Lernpfad „Von Öl zu Wachs in fünf Schritten“ mit Lesefortschritt (nur im Browser), Symptom-Wegweiser mit Antriebsgrafik und sechs Markierungen (Ursache, schnelle Lösung, Sprung in den Abschnitt; Vorbild iFixit/Park Tool), Empfehlung mit Vergleichsslider, „Die Zahlen“, Archiv mit Magazin-Rhythmus und Kategorie-Kurztexten. Inhalte in `src/pages/blog/hubContent.ts` (jede Aussage aus dem verlinkten Artikel); der Blog nutzt weiterhin kein `i18n.ts` (war schon vorher so). `scripts/check-search.mjs` prüft jetzt auch Antwortkarten, Tippfehler, Platzhalterfragen und dass jeder Symptom-Anker auf eine echte `<h2>` zeigt. Suche nach Kategorie filtert nicht mehr (Filter sitzen jetzt beim Archiv, nicht bei der Suche). **Nachtrag:** Artikelseiten zeigen die FAQ jetzt sichtbar (stand vorher nur im FAQPage-JSON-LD, Google verlangt sichtbaren Inhalt); die Antwortkarte springt auf `#faq-<frage>` und klappt sie auf. Die Wattzahl im Zahlenstreifen kommt aus `waxVsOil.watts` (2–4 W gegen 6–10 W bei 250 W, Link auf /wissenschaft) statt der Blog-Formulierung „4–5 W“. **Bewusst nicht angefasst:** die „Laborreferenz 300–400 W“-Stellen in `articles.ts` (offene Entscheidung oben); die Antwortkarte kann die betroffene FAQ-Antwort weiterhin zitieren, bis Luca entscheidet.
- 2026-09-15 · **Startseite `#warum-wachs`: „Du merkst es sofort" → „Was sich für dich ändert"** (Branch `feat/startseite-was-sich-aendert`, von `main` 4a049f1). Form von der Wachsseite (Kapitel 01) übernommen: großer Slider 6/5 mit Labels im Bild, vier Icon-Punkte statt 01–04, Kennzahlenreihe. Anders als dort: Foto/Mikroskop-Umschalter bleibt (Luca), Kennzahlen produktneutral (2–3× · 250–450 km · 2–4 W bei 300–400 W · 47 € mit Link zur Herleitung) statt Wattbalken + Kostenkarte, dazu „Wachs wählen" → `#produkte` mit Ab-Preis. „Es wird leise" raus (Luca). Punkte-Texte liegen jetzt einmal in `i18n whyWax.points`, Icons in `CHANGE_ICONS` (`Ico.tsx`), beide Seiten lesen daraus. Runde 2: Punkt 1/2 tragen Beleg-Links („Foto ansehen", „Unterm Mikroskop ansehen"), die den Slider umschalten (mobil mit Scroll dorthin), aktiver Punkt mit gefüllter Icon-Kachel; mobil kompakter (Sektion 2140 → ~1945 px bei 375 px) und ohne eigenen CTA-Button (die Sticky-Leiste führt schon zu den Produkten, nur die Preiszeile bleibt); Watt-Spannen brechen nicht mehr am Strich um; Reveal-Animation überspringt Listen, die beim Mount schon im Bild sind (Anker-Sprung).
- 2026-09-15 · **Mobile-Audit umgesetzt** (Branch `fix/mobile-audit`, Luca: „ohne etwas kaputt zu machen"). Gemessen live bei 375 px + Lighthouse mobil, abgesichert per Pixel-Diff (15 Routen × 375/768/1280 × hell/dunkel gegen `main`). (1) Eingaben mobil 16 px (kein iOS-Zoom) — als `text-[16px] sm:text-[14px]`, **nicht** `text-base`: dessen Zeilenhöhe 24 px überlebt `sm:text-[14px]` und machte Desktop-Felder 3 px höher. (2) Startchunk 603 → 533 KB: `WhyWax`, `CompareModal` (eigene Datei, lädt erst beim Klick, nimmt `productContent` mit) und `CartDrawer` lazy. `PendingAnchorScroll` wartet dafür 10 s statt 3 s — `#warum-wachs` kam kalt nach 3,3 s, der Sprung lief ins Leere. react-dom, i18n und gsap bleiben (285 KB, nicht teilbar ohne Risiko). (3) **Alle Kettenfotos lokal** (Luca freigegeben): sechs eBay-Hotlinks → `public/images/products/chains/<name>` mit AVIF/-card/-thumb/-lg über `build-avif-variants.mjs` (`CHAINS`-Liste); Regex in ChainCard/ProductDetailPage/generate-product-html auf `chains/[a-z0-9]+` verallgemeinert; ebayimg-preconnect raus. Kassetten-Diagramm AVIF + 800 px (218 → 52–94 KB). (4) A11y: Skip-Link-Ziel auf allen Seiten, Tabellen-Zeilenköpfe, Hotspot-Labels, „Passt dazu" als Überschrift, Trefferflächen (Footer 41 px mobil, Wissenschafts-Punkte 24 px, freie Textlinks per `py-2 -my-2`), Versand-Grün hell `#256B42` (5,5:1). (5) **12-px-Untergrenze mobil** (Luca) für Fließtext/Labels; **bewusst ausgenommen:** Grafik-Beschriftungen, Zahlen in Kreisen, Versal-Mikrolabels, Geschenkkarten-Mockup und die mm-Zeile im Wachs-Wetter (12 px bricht in den 36-px-Spalten bei 360 px um). Nicht gemacht: SSR/Hydration der Startseite (zu riskant mit GSAP/localStorage), Bewertungsfotos mit width/height (würde die Kartenhöhe auf 500 px zwingen).

- 2026-09-14 · **Rewax v2 live + 12 Stadtseiten + Wachs-Woche** (Branch `feat/rewax-v2`, Luca). `/kette-wachsen-lassen` neu: Hero als Bildbühne, Stufenformular (Leistung → Menge → Kontakt, API-Payload unverändert), Ablauf als drei Karten mit Zeitstrahl, Preise als eine Tabelle, Geschenk-Sektion (Karte als Objekt, Starter-Set „ab"-Preis, Link in den Konfigurator). **Stadtseiten** `/kette-wachsen-lassen/:stadt` (12 Städte, `src/pages/rewax/cities.ts`) — frühere Entscheidung „keine Stadtseiten" bewusst revidiert: jede Seite trägt eigene DWD-Zahlen (Niederschlag, Regentage 1991–2020 → Nachwachs-Intervall), sagt ehrlich „per Post aus Stuttgart", im Prerender echt verlinkt (Hub → 12 Städte, Stadt → 3 Nachbarn). **Wetter nur aus DWD-Opendaten** (CC BY 4.0): Open-Meteo ist für kommerzielle Seiten nicht erlaubt; Live-Woche über `api/weather.ts` → Bright Sky. **Kein Preisvergleich mit Mitbewerbern** (§ 6 UWG: Leistungen nicht deckungsgleich, Preise nicht überwachbar), stattdessen eigener Komplettpreis mit Porto (19,55 € / 28,55 €). Topbar zeigt auf Rewax-Seiten Service-Meldungen statt „versandkostenfrei". Umstieg-Betrieb von Luca bestätigt. Strava nur als Konzept (`docs/plaene/STRAVA_INTEGRATION.md`). Offen: GSC-Einreichung (Luca).
- 2026-09-14 · **Kettenseite, Anleitungen, Navigation mit Topbar** (Branch `feat/ketten-nav`, Lucas Screenshot-Feedback). (1) Anleitungen-Scrollbalken behoben: `overflow-x-hidden` machte die Accordion-Liste zum Scroll-Container, jetzt `[overflow-x:clip]`. (2) Kettenkarte: Foto 16:10 mit Chip „Mit Pro gewachst“ (Luca: alle Ketten mit Pro), Faktenzeile entfettet · Quick-Link · fahrbereit, grüne `ShippingPill` „Versandkostenfrei“ + Lieferdatum statt grauem Fußstreifen „inkl. Versand (eBay)“. Badges nur aus Daten: gepflegtes `badge` oder `cheapestChainIds` („Günstigste 12-fach“, bei Gleichstand mehrere). (3) `/ketten`-Kopf: Rück-Pille mit Pfeil-Stupser, Rückweg zusätzlich in der klebenden Filterleiste, vier Vertrauenskacheln (`chainTrust`) mit Live-Versandzeile (`useDispatchLine`, aus WaxHero herausgelöst). (4) „Passt dazu“: „Kettenwachs“ mit Pro-Bild und „Ab 22,95 €“ (`minWaxPrice`), Lieferdatum + Versand-Pille bei Wachs und Set, Rewax ohne Versandversprechen. (5) Anleitungen: Gesamtdauer je Ablauf im Kopf, rechte Karte „Auf einen Blick“ aus `waxProcessTimeline` statt handgeschriebener Minuten, drei Wege weiter. (6) Navigation nach yoeleo.com: Pillen mit einfahrendem Duplikat, Trefferfläche = volle Leistenhöhe, Klappen mit konkaven Ecken an der Headerunterkante (`NavMenu.tsx`), aktiv = Punkt statt Pille; Topbar (`Topbar.tsx`) mit Kontaktwegen, vier wechselnden Meldungen, Sprache/Hell-Dunkel, fährt ab 8 px Scroll weg — `body:has(.wx-topbar)` trägt ihre Höhe, keine Seite musste ihren Kopfabstand ändern. Versand-Grün jetzt global (`--ship-*`). Dunkelmodus: Topbar kehrt sich um (heller Streifen, `--topbar-*`, gleiche Umkehr wie `--cta-bg`), Klappen mit 1-px-Kontur, die per Bogenstrich aus der Header-Unterlinie wächst. Instagram in `CONTACT` (ohne Tracking-Parameter). Hinweis: „Einfahren 10–20 min“ ist aus der Startseitenkarte raus, weil es der Zeitleiste (1 min) widersprach.
- 2026-09-14 · **Wachsseite v5 + Regal-Karten** (Luca). Regal-Karte Classic/Pro nach der Kaufbox-Grammatik (Größen-Kacheln, großer Preis, Rabatt-Pille, Kauf unten rechts, §19 nur einmal unter dem Regal). Kapitel 01: Slider ohne Balken, Labels auf dem Bild, **Reibungszahl als Kachel raus** („< 1 Kette kostet der Block"). Kapitel 02: Kassette ohne weißen Kasten, Zoom-Linse. Kapitel 03: Kettenrotation 1/2/3 mit Empfehlung, „alle 1 Jahre"-Fehler behoben. Kapitel 04: **was während des Schmelzens geht, läuft parallel**, neuer Schritt „Kette montieren", Nachwachsen 34 min (4 Arbeit), Uhr zurück. Kapitel 07: gleich hohe Karten, GPSR als Tabellenzeile. **Saison-Linie seitenweit:** beide Wachse ganzjährig, Pro hält bei Nässe/Kälte länger, Dauerregen verkürzt jedes Intervall. **Lagerung:** keine Monatsangaben mehr (Wachs wird nicht schlecht). Mobil-Politur: Vergleichstabelle ohne Querscrollen (Öl-Spalte mobil aus), Bewertungen und Set/Ketten/Rewax als Wischband. Hook-Aussage in `CLAUDE.md`/`AGENTS.md` korrigiert (lokaler, nicht versionierter Pre-Commit-Hook).

- 2026-09-14 · **Rechner-Deck v5, Runde 2: die Anatomie hält jetzt messbar.** Runde 1 sah gleich aus, war es aber nicht: der Antwortblock stand je nach Karte zwischen 290 und 328 px (weil `mt-auto` nur die Unterkante hält), und die Kettenlänge war in „Messen" 26 px höher als in „Zählen" — das änderte über die Höchste-Karte-Regel die Höhe des ganzen Decks. Jetzt drei feste Zonen: Bild 186, Körper 216, Antwort 210 px; jede Zeile im Antwortblock mit fester Höhe, keine Bildzone mehr darin (das Kettenstück steht bei der Skizze). Nachgemessen über je 28 Zustände bei 1024/1280/1440 px und mobil: Kartenhöhe, Blockhöhe und Blockposition überall identisch, kein Überlauf, alle Grafiken 1:1. Dazu: Verschleiß-Methode als Segment (passte bei 1024 nicht in eine Zeile), Lehren-Chips ohne Einheit, toter „Wachs kaufen"-Knopf auf `/rechner/intervall` repariert, geteilte Links tragen den Kartenzustand (`k`, `at`), Kennzahl „Wachs + Werkzeug" statt „Einstieg" (der Set-Preis darunter ist ein anderer), gestrichelte Platzhalter in der Kettenliste raus, restliche deutsche Strings nach `i18n.ts`.
- 2026-09-14 · **Rechner-Deck v5: fünf gleich große Karten, flache Nachbarn, eine Grafik je Rechner** (Branch `feat/rechner-deck-v5`, Luca). Alle Karten liegen in einer Grid-Zelle, das Deck ist so hoch wie die höchste — kein Höhensprung mehr beim Blättern (vorher Höhe der aktiven Karte, 518–724 px). Nachbarn ohne `rotateY`: skaliert 0,86, abgedunkelt, Deckeltext im sichtbaren Streifen mit Abstand zur aktiven Karte. **Umstieg + Ersparnis → eine Karte „Kosten"** (`CostCalculator`: Antrieb Deore/XT/XTR + Ketten im Wechsel, Hantel-Grafik Öl gegen Wachs je Posten). `/rechner/umstieg` und `/rechner/ersparnis` bleiben als Seiten, im Deck nur noch fünf Karten (`TOOLS[].inDeck`). Verschleiß antwortet mit einem Wort und einer Skala statt „0,00 %", Intervall mit Zeitstrahl, Passende Kette als Liste mit Ausweg zum Umstieg-Service, Kettenlänge-Rechenweg im Popover. Neuer Status-Token `--warn`. Kettenlänge rechnet exakt `2 × mm ÷ 12,7` (Park-Tool) statt 0,157 — ändert das Ergebnis in rund 11 % der Eingaben um 2 Glieder. Sonst keine Rechenlogik geändert. Details: `docs/DESIGN.md` §7.
- 2026-09-14 · **Kettenlaufzeit im Rechner an Blog/FAQ angeglichen, Versand immer kostenlos, Versandschluss 15 Uhr** (Luca, 13.09.2026). `OIL_CHAIN_KM` 4.000 → 3.000, `WAX_CHAIN_KM` [6.000, 8.500, 10.500] → [7.500, 9.500, 11.000]: vorher rechnete der Rechner 1,5× und widersprach dem eigenen Claim „2–3×“ und den Praxiswerten im Blog (Öl 2.000–3.000, Wachs 6.000–12.000 km). Alle Sparbeträge der Seite steigen dadurch. Neu `DRIVETRAIN_CLASSES` (Einsteiger/Mittelklasse/High-End als Richtwerte) und optionale Teilepreise in `drivetrainCosts()`. `freeFromCents = 0`, alle Versandaussagen „versandkostenfrei“, `dispatchStatus()` für die Live-Versandzeile. Die Rotationsfrage (Kilometer je Kette bei 2/3 Ketten) bleibt inhaltlich offen, Werte nur proportional mitgezogen.
- 2026-09-13 · **Rechner-Deck: Bildschirmhöhen-Regel aufgehoben, Skizzen zurück, Umstieg neu.** Die Regel „Sektion passt auf eine Bildschirmhöhe" (f26cf83) hatte Skizzen, Kettenliste, Einkaufsliste und Aufschlüsselung aus den Karten gedrängt — die Rechner beantworteten die Frage, halfen aber nicht. Jetzt: Deckhöhe = aktive Karte (gemessen, animiert; mobil ebenso), Mitte 58–64 % breit, Nachbarn mit Außenkante nach hinten gedreht und lesbarem Deckel im sichtbaren Streifen. Neue `sketches.tsx`: Live-Antriebsskizze + sichtbarer Rechenweg (Kettenlänge), Zähl-Modus „Alte Kette zählen", Kettenstück mit abzunehmenden Gliedern, Lehren-/Messskizze (Verschleiß), Kassetten-Skizze (Ersparnis), Kostenkurve Öl vs. Wachs mit Break-even (Umstieg). Umstieg fragt jetzt „Ab wann lohnt sich Wachs?" (Heldenzahl = Monate bis im Plus). Passende Kette zeigt die Treffer als Produktkarten. Zweispaltig per Container-Query (`.cq-split`), wo die Karte ≥ 600 px breit ist. Zahlen/Annahmen in `waxMath.ts` unverändert von dieser Änderung — die Kettenlaufzeit-Korrektur im Eintrag darüber kam separat von Luca und ist hier bereits eingerechnet.
- 2026-09-13 · **Produktkarten-Umbau, Stufe 3–4 gebaut, Plan abgeschlossen** (`docs/plaene/PRODUKTKARTEN_PLAN.md`, alle fünf Stufen ✅). Stufe 3: `ChainCard` aus `products.tsx` in eine eigene Route `/ketten` (`KettenPage.tsx`) herausgelöst statt eines `useState`, der das Regal ersetzt; `?marke=`/`?gang=`-Filter über `useSearchParams`, sticky Filterleiste (Desktop-Reihe, Mobile-Bottom-Sheet), Null-Treffer-Zustand mit Reset, „Passt dazu"-Reihe (Wachs/Starter-Set/Rewax); gemeinsames `ketten/content.ts` für React-Seite und Vorrender-Skripte (Muster wie `rewax/content.ts`) hält Meta/JSON-LD synchron; `backTarget()` ersetzt das hartkodierte „Produkte"-Breadcrumb auf der Produktseite durch „Alle Ketten"/„Alle Produkte" je nach Kategorie; alte `/?ketten=marke-gang`-Deep-Links leiten auf `/ketten` um; Sitemap/llms.txt/404-Seite verlinken `/ketten`. Stufe 4: `viewTransition`-Prop auf den Kartenlinks (React Router 7.15, gegen `prefers-reduced-motion` gegattert), erste Bildreihe `fetchpriority="high"`+eager statt lazy (`priority`-Prop durchgereicht), `aria-pressed` auf allen Filter-Chips (`TogButton`), 160-ms-Überblendung bei Filterwechsel per CSS-Keyframe und React-`key`-Remount. 44-px-Trefferflächen und der Kontrast-/Dark-Mode-Durchgang waren bereits ab Stufe 1/3 erledigt; „scroll-margin-top gegen die klebende Navigation" entfällt, weil `/ketten` keine In-Page-Anker hat, zu denen gesprungen wird. **Bewusst ausgelassen:** die aufklappbare `QuantityDiscountChip`-Staffelanzeige aus dem Regal wurde nicht auf die Produktdetailseite zurückportiert — dort bleibt der bestehende `bundleOffer`-Satz, jetzt korrekt an `checkoutEnabled` gekoppelt. **Offen für Luca (unverändert seit Stufe 2):** Bewertungszahlen je Kette, Konflikt 20–32 gegen 15–20 Anwendungen je Block, Versand-Kontoeinstellung im Google-Merchant-Center, sechs von acht Kettenfotos fehlen noch als lokale AVIF/`-card`-Variante (laufen weiter über eBay-Hotlinks ohne `<picture>`).
- 2026-09-13 · **Produktkarten-Umbau, Stufe 0–2 gebaut** (`docs/plaene/PRODUKTKARTEN_PLAN.md`), Pflicht-Zwischenstopp erreicht. Stufe 0: `.num-data` → `.num` in allen Verkaufsflächen, Mono bleibt nur in `sections/science/`, `InstrumentFrame`, Blog-Code und an echten Messwerten auf `SciencePage.tsx`; eine Sternkomponente (`Stars.tsx`, anteilige Füllung per clip-path) ersetzt drei; `CLAUDE.md`/`AGENTS.md` Regel 5 korrigiert (`tsc -b --force`, kein Pre-Commit-Hook). Stufe 1: `ChainCard` auf `.shelf-card`-Grammatik, Stretched-Link statt `<button>` in `<a>` (K2), `<picture>`-AVIF-Pipeline wo Bilder existieren (nur `hg701`/`ybn11` lokal — sechs von acht Kettenfotos bleiben eBay-Hotlinks ohne eigene Größen, das ist eine fehlende Bildressource, keine Stufe-1-Entscheidung), Klartext statt Spec-Pills, Raster 1/2/3/4 Spalten (K1). Stufe 2: Versandaussage an `checkoutEnabled` gekoppelt an allen ~14 Fundstellen plus einer beim Bauen gefundenen 15. (`bundleOffer`-Satz „und damit versandkostenfrei" auf der Produktseite, lief bisher ungated); `perApplicationRange()`/`waxTierBreakdown()` in `data.ts` liefern Preis-je-Anwendung und aufklappbare Stückpreise in Cent gerechnet; Lieferdatum lang auf der Produktseite, Turnaround statt Lieferdatum bei Rewax und der Rewax-Kachel; `preWaxedHint` ersatzlos gelöscht (DE/EN); Rewax-Preiszeile „15,95 € je Kette" / „mit 10er-Karte 9,45 €". Bewertungsfelder je Kette existieren bereits als optionale Felder in `data.ts`, bei keiner Kette gesetzt — Sterne rendern nur bei echten Zahlen. **Offen für Luca:** Bewertungszahlen je Kette, Konflikt 20–32 gegen 15–20 Anwendungen je Block, Versand-Kontoeinstellung im Google-Merchant-Center, sechs fehlende Kettenfotos in AVIF/`-card`-Größe. Stufe 3 (`/ketten`-Route) und Stufe 4 (Politur) stehen noch aus.
- 2026-09-12 · **Produktkarten-Umbau geplant, noch nicht gebaut** (`docs/plaene/PRODUKTKARTEN_PLAN.md`, Prompt daneben). Lucas Screenshot-Feedback auf fünf Ursachen zurückgeführt, als fünf Stufen ausgearbeitet. Entschieden: IBM Plex Mono verlässt alle Verkaufsflächen und bleibt nur in den Wissenschafts-Figuren und Blog-Codeblöcken, wo Gleichbreite Bedeutung trägt; eine Kartensprache (`.shelf-card`) für Regal und Kettenliste; die Kettenliste wird die Route `/ketten` statt eines `useState`, der das Regal ersetzt; Lieferdatum auf den Karten, Mechanismus auf der Produktseite, Turnaround statt Lieferdatum bei Rewax; Rewax-Preis als „15,95 € je Kette" mit „mit 10er-Karte 9,45 €" darunter statt „ab 15,95 €". Lucas Antworten: eBay-Versand ist im Preis enthalten, Staffel bleibt 5/10/15 %, Turnaround 3–5 Werktage, echte Kettenbewertungen existieren. **Fünf Befunde nebenbei, alle belegt:** (1) `preWaxedHint` behauptet „voller Käuferschutz", laut Luca falsch, wird gelöscht. (2) Die Versandaussage „zzgl. Versandkosten, ab 50 € versandfrei" steht an rund 14 Stellen inklusive JSON-LD und Merchant-Feed, beschreibt aber den eigenen Checkout, der wegen fehlender `stripePriceId` nachweislich aus ist — sie wird an `checkoutEnabled` gekoppelt und kippt beim Stripe-Start von selbst zurück. (3) `CLAUDE.md` Regel 5 ist doppelt falsch: `npx tsc --noEmit` prüft nichts (Wurzel-`tsconfig.json` hat `"files": []`) und einen Pre-Commit-Hook gibt es nicht; richtig ist `npx tsc -b --force`, wie `docs/SEO_TECHNIK.md` längst schreibt. (4) `ChainCard` legt einen `<button>` in einen `<a>` — ungültiges HTML, wird durch das Stretched-Link-Muster ersetzt. (5) Die Kettenbilder laufen an der AVIF-Pipeline vorbei und sind alle lazy. **Nicht die Staffel ist der Preishebel beim Wachs**, sondern der Preis je Anwendung (ca. 0,95 bis 1,50 € je Wachsvorgang) — 5 % auf zwei Blöcke sind 3,00 €, dafür lohnt keine Tabelle. **Offen (Luca):** Bewertungszahlen je Kette, Konflikt 20–32 gegen 15–20 Anwendungen je Block, Versand-Kontoeinstellung im Google-Merchant-Center.

- 2026-09-07 · **Rewax-Seite: zweite Leistung, Preis-Anhebung, nationale Sichtbarkeit.** (1) **Zwei Leistungen** statt einer: **Auffrischung** (schon gewachste Kette) und **Umstieg** (geölte/neue Kette entfetten + erstmals wachsen). Die "keine öligen Ketten"-These flippt: geölte Ketten kommen zuerst in ein *separates* Lösemittelbad, das Wachsbad sieht nie Öl. **Betriebs-Gate:** `UMSTIEG_LIVE` in `src/pages/rewax/content.ts` schaltet den ganzen Umstieg-Teil aus, wenn der Betrieb das (noch) nicht leistet — die Seite bleibt dann reine Auffrischung mit allen anderen Verbesserungen. (2) **Preise:** Auffrischung 13,95 → **15,95 €** einzeln, 9,95 → **11,95 €** ab 3. Umstieg **24,95 €** / **21,95 €** ab 3 (~10 € unter Kettenhelden 39,95 € / bikeoptimierung 34,90 €; +9 € über der Auffrischung = echter Entfetter-Mehraufwand). Karten-Anker rechnet auf 15,95 nach; Karten zeigen nur Euro-Ersparnis, keinen Prozentsatz. (3) **Geteiltes Modul** `src/pages/rewax/content.ts` (Preise, FAQ, Meta, JSON-LD) → Seite + Prerender wortgleich. (4) **SEO:** Service-Schema neu (`provider` per `@id` an `#organization`, `areaServed` als `Country`, `hasOfferCatalog` mit beiden Leistungen); Service + FAQPage jetzt auch im Prerender; og/twitter im `<Helmet>` repariert (gingen bei der Hydration verloren); Sitemap `/kette-wachsen-lassen` auf `priority 1.0` / `weekly`. (5) **Nationale Reichweite ohne Doorway-Seiten:** ein "Aus ganz Deutschland einschicken"-Abschnitt nennt Hamburg, München, Köln, Berlin, Leipzig ehrlich als Wohnorte; **Leipzig als Standort ist tot** (D-M2 entschieden — nur Stuttgart). (6) **Conversion:** Nutzen-Zeile mit Preis mobil above the fold, Leistungswahl als Rückgrat, Trust-Sektion (Luca + echte eBay-Zitate + "zurück in 3–5 Werktagen"), seiten-lokale Mobile-Sticky-CTA, "keine Zahlung jetzt"-Zeile, interne Links raus. (7) `api/rewax-request.ts` bekommt `service`. Off-Site-Aktionsliste: `docs/plaene/REWAX_SICHTBARKEIT.md`. **Offen (Luca):** Betriebsbereitschaft Entfetten · Turnaround-Zahl · besseres Luca-Foto.

- 2026-09-06 · **Rewax-Seite überarbeitet (`RewaxPage.tsx`).** (1) Layout: Kopf über die volle Breite, darunter Formular + Bild nebeneinander mit fluchtender Oberkante (`lg:items-start` statt `-center`), kompakter Ablauf + Preis-Kurzfassung neben dem Formular — erster Viewport zeigt Bild, Formular, Ablauf, Preis auf einmal. Foto-Ablauf nur noch unter `lg` in der Preis-Sektion (sonst doppelt). (2) Stempelkarten sind **All-in**: Preis deckt Wachsen + Rückversand. Rabatt misst sich am **Einzelpreis 13,95 €** statt am Dreierpreis. **5er 49,75 € (spart 20 €), 10er 94,50 € (spart 45 €)**; ersetzt 44,78 € / 84,58 €. Selbstkosten-Annahme je Vorgang 3–5 € ohne Porto. (3) Karten-Text von vier auf zwei Zeilen; Einlöse-Erklärung einmal unter beiden Karten + als FAQ. (4) Stempel-Animation langsamer (`STAMP_STAGGER_MS` 550 → 950), Overshoot weicher. (5) Geschenk-Vorschau-Popup (`GiftPreviewModal.tsx`, Muster wie `CompareModal`) mit Karten-Mockup + Starter-Set als Beigabe. (6) Bild-Band-CTA am Seitenende **entfernt** — redundant zum Hero. (7) `mailLink` mit entfernt (nur dort benutzt).
- 2026-09-06 · **Starter-Set: viertes Bundle „ohne Kette" + drittes „HG701" + Classic auf 300 g.** `StarterSetOption.chainId` ist jetzt optional. Neu `starter-nochain` (300 g Classic + Zange + Draht, **keine Kette**) → **27,92 €**, günstigstes Bundle überhaupt, für alle mit schon vorhandener wachsbarer Kette. `minSetPrice` (Homepage-Kachel) rechnet die kettenlose Untergrenze mit → „Ab 27,92 €". `i18n.setBody`, Meta-Description (`StarterSetPage.tsx` + `generate-blog-html.mjs`) auf „Kette optional dazu" umformuliert. Neu `starter-hg701` (500 g Classic + Shimano Ultegra HG701) → 72,04 €. `starter-classic` von 500 g auf **300 g Classic** (22,95 € statt 29,95 €) → Set 57,63 € statt 63,58 €: das Set ist der Einstieg für Erstwachser, niedrigerer Einstiegspreis konvertiert besser, 300 g reichen fürs erste Jahr, und 300 g Classic / 500 g Pro trägt die Saison- vs. Ganzjahres-Positionierung. `discountPct` bleibt 15. Kachelraster jetzt `sm:grid-cols-2` (4 Karten als 2×2), Konfigurator-Umschalter als volle Zeile darunter. Draht bleibt 4,95 € (die genannten „4,59 €" waren eine Verwechslung). Preisleiter: 27,92 / 57,63 / 72,04 / 84,83 €.

- 2026-08-13 · **Englischer Zweig eingefroren, nicht ausgebaut.** Die Sprachumschaltung laeuft ueber `localStorage` ohne eigene URL, es gibt kein `hreflang`, `index.html` steht fest auf `lang="de"`. Damit ist die gesamte englische Textarbeit (`titleEn`, `descriptionEn`, der englische Teil von `i18n.ts`) fuer Google und jeden KI-Crawler unsichtbar. Entscheidung: **so lassen**. Die realistischen Ranking-Chancen liegen alle im Deutschen, und ein zweiter Markt kostet doppelte Pflege, bevor der erste laeuft. Der Umschalter bleibt als Bedienkomfort. **Wieder aufmachen, wenn:** der eigene Checkout laeuft und messbar Auslandsbestellungen kommen — dann `/en/`-Routen mit `hreflang` in beide Richtungen.
- 2026-09-06 · **Repo verschlankt, Runde 2.** `git gc` (`.git` 360 → 274 MB, loser Objekt-Müll gepackt). Toter Code raus: `src/components/viz/illustrations/` (Chain/SprocketTooth/CassetteHero, nie konsumiert), tote Exports `SectionHeader`, `useScrollReveal`, `waxTiers`, `DIVE_GRAPH`, `gramsPerApplication`, die fertige `scripts/migrate-chain-images.mjs`. Verwaiste Bilder: `waxcelerate-mark.svg`, `doors/{ketten,kettenwachs}*`, `shelf/{chains-flat,starter-box}*`, ein Blog-Doppelsuffix-Artefakt (dist-Bildreferenzen vorher/nachher identisch). `.cursorrules` gelöscht (dupliziert CLAUDE.md, kein Cursor im Einsatz), `kimi-plugin-inspect-react` aus `devDependencies` (−108 transitive Pakete). Erledigte Handoffs (`LOGO_MARK_SVG_HANDOFF`, `DEPLOY_HANDOFF`) nach `docs/archiv/`, `SKILL_PREISE_UPDATE.md` nach `docs/aufgaben/`, `docs/archiv/info.md` gelöscht, `PROJECT.md`-Index geschlossen, Fakten-Drift korrigiert. **Bewusst geparkt gelassen:** `comparison-slider.tsx`, `WhatChanges.tsx` (AGENTS.md Falle #7). **Branches unangetastet.**
- 2026-08-13 · **Motion-Briefs verworfen** (`docs/archiv/LOTTIE_MOTION_BRIEFS.md`). Nie gebaut, wuerden funktionierende SVG-Diagramme durch eine neue Abhaengigkeit ersetzen. Reversibel, Briefs bleiben liegen.
- 2026-08-11 · **Repo aufgeräumt.** Doku nach Lebenszyklus in `docs/` sortiert (Wurzel: 27 → 4 Markdown-Dateien). `assets-archiv/` (172 MB) aus dem Repo heraus nach `../waxcelerate-assets-archiv/`. Verwaiste Arbeitskopien unter `.claude/worktrees/` (219 MB) gelöscht, die drei ungespeicherten SciencePage-Experimente daraus als Patches in `docs/archiv/worktree-experimente-2026-06/` gesichert. `functions/` (Cloudflare Pages) entfernt — die Seite läuft auf Vercel, der Ordner wurde nie ausgeführt.
- 2026-08-11 · **`/admin` war in Produktion kaputt** und ist repariert. `AdminPage.tsx` rief `/api/admin`, den Endpunkt gab es aber nur als Cloudflare-Function in `functions/api/admin.ts`. Auf Vercel greift ausschließlich `api/`, also lief die Bestandsverwaltung ins Leere. Jetzt portiert als `api/admin.ts`. Ohne gesetztes `ADMIN_PASSWORD` antwortet der Endpunkt bewusst mit 503 statt jeden hereinzulassen.
- 2026-08-11 · **Der Stop-Hook, der `git add -A && git commit && git push` ausführte, ist entfernt** (`.claude/settings.json`). Er hat jede Sitzung ungeprüft committet und gepusht. Wer ihn zurückholt, sollte wissen, dass er auch Halbfertiges veröffentlicht.
- 2026-08-11 · `api/` wird von keiner `tsconfig` erfasst und damit **weder von `tsc -b` noch vom Build geprüft**. Bekannt, nicht behoben. Bis dahin Serverless-Änderungen von Hand prüfen: `npx tsc --noEmit --skipLibCheck --strict --module esnext --moduleResolution bundler --target es2022 --types node api/*.ts`

- 2026-07-29 · Hero-Bild entschieden: Variante A, hängende Ketten. Rotation verworfen. Begründung in `docs/DESIGN.md` §6, Einbau liegt in Todoist auf Mittwoch.
- 2026-07-29 · Rewax-CTA ist ein Bildband über die volle Breite statt eines blassen Kastens, ein Knopf statt zwei. Geschenk-Hinweis sitzt auf der Stempelkarte selbst.
- 2026-07-29 · Bewertungskarten zeigen das Kundenfoto als 16:9-Band oben, Text bleibt auf der Kartenfläche. Laufschrift bleibt.
- 2026-07-29 · Wachs-Staffel 2/3/5 Stueck = 5/10/15 Prozent, ausschliesslich auf Wachs. Prozent-Badge von den Produktkarten entfernt, Staffel steht einmal ruhig ueber der Liste.
- 2026-07-29 · Starter-Set als Konfigurator: zwei Fragen, Zange und Draht liegen automatisch bei. 15 Prozent unter der Teilesumme, angezeigt als Euro-Ersparnis statt als Prozentzahl.
- 2026-07-29 · Zubehoer als eigener Typ `Accessory` statt als `Product`, weil Zubehoer keine Intervalle, Kompatibilitaeten oder Bewertungen hat und nicht in die Produktfilter gehoert.
- 2026-07-29 · `/rewax`, `/starter-set` und `/wissenschaft` werden vorgerendert und stehen in der Sitemap.
- 2026-07-29 · `chain-dirty.jpg` und vier tote Zwischenstaende geloescht.
- 2026-07-29 · FAQ und Kontakt: Lesemass 880px nur auf dem Inhaltsblock, Ueberschrift bleibt an der Spaltenkante.
- 2026-07-29 · /starter-set gebaut. Rabatt als Rechnung gezeigt, nie als Prozent-Badge.
- 2026-07-29 · Produktliste erscheint erst nach Klick auf eine Tuer, mit Rueckweg.
- 2026-07-29 · Zehnerkarte auf /rewax statt als vierte Produkttuer.
- 2026-07-29 · Rewax-CTA geht direkt auf WhatsApp mit vorbefuelltem Text, E-Mail als Alternative.
- 2026-07-29 · KI-Bilder process-melt.jpg und process-dip.jpg ersetzt durch echte Aufnahmen (public/images/rewax/)
- 2026-07-28 · „Was das bedeutet" ist kein Diagramm mehr, sondern drei Zeilen mit Foto und je einer Aussage (`WhatChanges.tsx`). Zwei Diagrammversuche davor sind gescheitert, weil beide erst dekodiert werden wollten.
- 2026-07-28 · Kettenglied im Schnitt: die Kette steht jetzt aufrecht links, damit die Schnittebene waagerecht in die Zeichnung läuft
- 2026-07-28 · `/rewax` gebaut: eigene Seite, Navigation, Band unter der Produktliste. Nur Rewax bereits gewachster Ketten, kein Entfetten.
- 2026-07-28 · Preise Rewax B2C: 13,95 € einzeln, 9,95 € ab drei Ketten, plus 1,80 € Rückversand. **Ersetzt die älteren 9,99 / 24,99 im Business-Kontext-Skill, das gehört dort nachgezogen.**
- 2026-07-28 · Eine Aussage pro Fläche. `waxVsOil.life` trägt jetzt eine Spanne, überall wird „2 bis 3×" gerendert, nie mehr „3×"
- 2026-07-28 · `InstrumentFrame` hell steht auf Weiß statt `--sf2`, `grain` nur noch in der dunklen Variante. Panel ist Papier, nicht ein weiterer Grauton der Seite.
- 2026-07-28 · Figuren bekommen zwei Layouts statt einer Skalierung, weil SVG-Text mit der viewBox schrumpft. (Der damalige Helfer `src/hooks/useMediaQuery.ts` wurde am 11.08.2026 entfernt — er hatte keinen einzigen Aufrufer mehr. Die Entscheidung gilt weiter, sie wird heute über Tailwind-Breakpoints umgesetzt.)
- 2026-07-28 · Produkteinstieg über drei Türen nach Absicht statt Produktliste (`ProductDoors.tsx`)
- 2026-07-28 · Zeichenskala als Tokens `--dw-hair/line/bold`, Mindestschrift 11 px in Figuren (`docs/DESIGN.md`)
- 2026-07-28 · Grain-Overlay auf 2 % und `color-interpolation-filters=sRGB`, behebt den Olivstich
- 2026-07-28 · Antriebsverlust als Sägezahn statt Balken oder Strichkamm (`DrivetrainLoss.tsx`)
- 2026-07-28 · ACT I Kontaktzonen ergänzt, behebt den toten Anker `#problem`
